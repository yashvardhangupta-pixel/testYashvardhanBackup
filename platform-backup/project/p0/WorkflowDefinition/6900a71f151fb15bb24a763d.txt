{
	"appsUsed":[
		
	],
	"createdTime":1761650463170,
	"deleted":false,
	"deployedVersion":91,
	"deploymentState":{
		"deployedAt":1784789606323,
		"deployedBy":-1,
		"deployedDefinitionId":"6a61ba66ea75a53e4acbeb8d",
		"status":"DEPLOYED",
		"version":91,
		"workflowVersion":231
	},
	"edges":[
		{
			"fromNodeId":"n_Ff9PD",
			"id":"next@n_Ff9PD@n_4hXDW",
			"priority":0,
			"skip":false,
			"toNodeId":"n_4hXDW",
			"type":"next"
		},
		{
			"fromNodeId":"n_4hXDW",
			"id":"next@n_4hXDW@n_4gk0w",
			"priority":0,
			"skip":false,
			"toNodeId":"n_4gk0w",
			"type":"next"
		},
		{
			"fromNodeId":"n_4gk0w",
			"id":"next@n_4gk0w@n_LUkEA",
			"priority":0,
			"skip":false,
			"toNodeId":"n_LUkEA",
			"type":"next"
		},
		{
			"fromNodeId":"n_LUkEA",
			"id":"next@n_LUkEA@n_VdssN",
			"priority":0,
			"skip":false,
			"toNodeId":"n_VdssN",
			"type":"next"
		}
	],
	"grants":{
		"configs":[
			{
				"permissions":[
					"VIEW_RUNS",
					"V"
				],
				"principalType":"GLOBAL"
			}
		]
	},
	"id":"6900a71f151fb15bb24a763d",
	"lastModifiedBy":200377,
	"lastPlatformUpdateBy":200377,
	"lastPlatformUpdateOn":1787141033062,
	"lcName":"best practices | check group | page",
	"modifiedTime":1787141033062,
	"name":"Best Practices | Check Group | Page",
	"nodes":[
		{
			"context":{
				"appName":"callables",
				"resourceVersion":1033,
				"resourceName":"callables_from_interface"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_uv02c-1",
			"id":"n_Ff9PD",
			"index":1,
			"inputs":{
				"result":{
					"additionalProperties":false,
					"type":"object",
					"required":[],
					"ua:modal":{
						"field_type":"STRING",
						"is_optional":true
					},
					"properties":{
						"checks":{
							"type":"array",
							"items":{
								"type":"object",
								"properties":{
									"check_id":{
										"type":"string",
										"title":"check_id"
									},
									"status":{
										"type":"string",
										"title":"status"
									},
									"targets":{
										"type":"array",
										"items":{
											"type":"object",
											"properties":{
												"path":{
													"type":"array",
													"items":{
														"type":"string"
													},
													"title":"path"
												},
												"targetId":{
													"type":"string",
													"title":"targetId"
												},
												"targetType":{
													"type":"string",
													"title":"targetType"
												}
											},
											"additionalProperties":false,
											"required":[]
										},
										"title":"targets"
									}
								},
								"additionalProperties":false
							},
							"title":"checks"
						}
					},
					"dynamic":true
				},
				"callableInterfaceId":"6900b0ef1cd6437e8860e8b2",
				"setup":{
					"additionalProperties":false,
					"type":"object",
					"required":[],
					"ua:modal":{
						"field_type":"STRING",
						"is_optional":true
					},
					"properties":{
						"check_id":{
							"type":"string",
							"title":"check_id"
						},
						"dataSources":{
							"type":"array",
							"items":{
								"type":"object",
								"properties":{
									"createdTime":{
										"type":"integer",
										"title":"createdTime"
									},
									"deleted":{
										"type":"boolean",
										"title":"deleted"
									},
									"deployedEntityVersion":{
										"type":"integer",
										"title":"deployedEntityVersion"
									},
									"entityType":{
										"type":"string",
										"title":"entityType"
									},
									"entityVersion":{
										"type":"integer",
										"title":"entityVersion"
									},
									"id":{
										"type":"string",
										"title":"id"
									},
									"lastModifiedBy":{
										"type":"integer",
										"title":"lastModifiedBy"
									},
									"modifiedTime":{
										"type":"integer",
										"title":"modifiedTime"
									},
									"ownerUserId":{
										"type":"integer",
										"title":"ownerUserId"
									},
									"properties":{
										"type":"object",
										"title":"properties"
									},
									"standard":{
										"type":"boolean",
										"title":"standard"
									},
									"version":{
										"type":"integer",
										"title":"version"
									}
								},
								"additionalProperties":false
							},
							"title":"dataSources"
						},
						"interfacePage":{
							"type":"object",
							"properties":{
								"createdTime":{
									"type":"integer",
									"title":"createdTime"
								},
								"deleted":{
									"type":"boolean",
									"title":"deleted"
								},
								"deploymentState":{
									"type":"object",
									"properties":{
										"deployedAt":{
											"type":"integer",
											"title":"deployedAt"
										},
										"deployedBy":{
											"type":"integer",
											"title":"deployedBy"
										},
										"entityVersion":{
											"type":"integer",
											"title":"entityVersion"
										},
										"version":{
											"type":"integer",
											"title":"version"
										}
									},
									"title":"deploymentState",
									"additionalProperties":false
								},
								"entityType":{
									"type":"string",
									"title":"entityType"
								},
								"id":{
									"type":"string",
									"title":"id"
								},
								"lastModifiedBy":{
									"type":"integer",
									"title":"lastModifiedBy"
								},
								"modifiedTime":{
									"type":"integer",
									"title":"modifiedTime"
								},
								"ownerUserId":{
									"type":"integer",
									"title":"ownerUserId"
								},
								"properties":{
									"type":"object",
									"title":"properties"
								},
								"standard":{
									"type":"boolean",
									"title":"standard"
								},
								"version":{
									"type":"integer",
									"title":"version"
								}
							},
							"title":"interfacePage",
							"additionalProperties":false
						},
						"globalInterfacePage":{
							"type":"object",
							"properties":{
								"id":{
									"type":"string",
									"title":"id"
								},
								"properties":{
									"type":"object",
									"title":"properties"
								}
							},
							"additionalProperties":false
						},
						"globalDataSources":{
							"type":"array",
							"items":{
								"type":"object",
								"properties":{
									"id":{
										"type":"string",
										"title":"Id"
									},
									"properties":{
										"type":"object",
										"properties":{},
										"additionalProperties":false,
										"title":"Properties"
									}
								},
								"additionalProperties":false,
								"required":[]
							},
							"title":"Global Data Sources"
						},
						"interfaceRecord":{
							"type":"object",
							"properties":{
								"id":{
									"type":"string",
									"title":"id"
								},
								"properties":{
									"type":"object",
									"title":"properties"
								}
							},
							"additionalProperties":false
						}
					}
				}
			},
			"skip":false,
			"subTitle":"Callable",
			"title":"Trigger interface",
			"trigger":{
				"type":"CALLABLE"
			},
			"type":"START"
		},
		{
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":1072,
				"resourceName":"variable_by_unifyapps_create_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_uv02c-1",
			"id":"n_4hXDW",
			"index":2,
			"inputSchema":{
				"type":"SCHEMA_AND_LAYOUT",
				"dynamic":false,
				"layout":{
					"createList":{
						"ui:title":"",
						"ua:type":{
							"ui:widget":"hidden"
						},
						"items":{
							"ui:title":"Initial list items",
							"ui:description":"Set the initial values of the list (defaults to empty)"
						},
						"ui:order":[
							"*",
							"items"
						]
					},
					"ua:title":{
						"ui:widget":"hidden"
					},
					"ua:list_type":{
						"ui:widget":"hidden"
					}
				},
				"schema":{
					"allOf":[
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"OBJECT"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"type":"object",
										"properties":{
											"items":{
												"type":"object",
												"properties":{}
											}
										}
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"STRING"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"type":"object",
										"properties":{
											"source":{
												"type":"string",
												"format":"ua:source",
												"title":"List of String"
											},
											"items":{
												"type":"string"
											}
										}
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"INTEGER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"type":"object",
										"properties":{
											"source":{
												"type":"string",
												"format":"ua:source",
												"title":"List of Integers"
											},
											"items":{
												"type":"integer"
											}
										}
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"NUMBER"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"type":"object",
										"properties":{
											"source":{
												"type":"string",
												"format":"ua:source",
												"title":"List of Numbers"
											},
											"items":{
												"type":"number"
											}
										}
									}
								}
							}
						},
						{
							"if":{
								"properties":{
									"ua:list_type":{
										"const":"BOOLEAN"
									}
								}
							},
							"then":{
								"properties":{
									"createList":{
										"type":"object",
										"properties":{
											"source":{
												"type":"string",
												"format":"ua:source",
												"title":"List of Booleans"
											},
											"items":{
												"type":"boolean"
											}
										}
									}
								}
							}
						}
					],
					"format":"ua:list",
					"type":"object",
					"properties":{
						"ua:title":{
							"type":"string"
						},
						"ua:list_type":{
							"type":"string",
							"title":"List Type",
							"oneOf":[
								{
									"const":"OBJECT",
									"title":"Object"
								},
								{
									"const":"STRING",
									"title":"String"
								},
								{
									"const":"NUMBER",
									"title":"Number"
								},
								{
									"const":"INTEGER",
									"title":"Integer"
								},
								{
									"const":"BOOLEAN",
									"title":"Boolean"
								}
							],
							"default":"OBJECT"
						},
						"createList":{
							"type":"object",
							"properties":{
								"ua:type":{
									"type":"string",
									"default":"mappedArray"
								},
								"source":{
									"type":"string",
									"format":"ua:source",
									"title":"List Item Source"
								},
								"items":{
									"type":"object",
									"properties":{
										"check_id":{
											"type":"string",
											"title":"check_id"
										},
										"status":{
											"type":"string",
											"title":"status"
										},
										"targets":{
											"type":"array",
											"items":{
												"type":"object",
												"properties":{
													"targetId":{
														"type":"string",
														"title":"targetId"
													},
													"targetType":{
														"type":"string",
														"title":"targetType"
													}
												},
												"additionalProperties":false
											},
											"title":"targets"
										}
									},
									"additionalProperties":false
								}
							}
						}
					}
				}
			},
			"inputs":{
				"createList":{
					"ua:type":"mappedArray"
				},
				"ua:title":"checks",
				"ua:list_type":"OBJECT"
			},
			"outputSchema":{
				"dynamic":false,
				"schema":{
					"type":"object",
					"properties":{
						"items":{
							"type":"array",
							"title":"checks",
							"disableActions":true,
							"items":{
								"type":"object",
								"properties":{
									"check_id":{
										"type":"string",
										"title":"check_id"
									},
									"status":{
										"type":"string",
										"title":"status"
									},
									"targets":{
										"type":"array",
										"items":{
											"type":"object",
											"properties":{
												"targetId":{
													"type":"string",
													"title":"targetId"
												},
												"targetType":{
													"type":"string",
													"title":"targetType"
												}
											},
											"additionalProperties":false
										},
										"title":"targets"
									}
								}
							}
						},
						"_ua_variableDetails":{
							"items":{
								"properties":{
									"dataType":{
										"description":"Data type of the variable",
										"type":"string"
									},
									"description":{
										"description":"Description of the variable",
										"type":"string"
									},
									"name":{
										"description":"Name of the variable",
										"type":"string"
									}
								},
								"type":"object"
							},
							"title":"Variable Details",
							"type":"array"
						}
					}
				}
			},
			"skip":false,
			"subTitle":"Variable by UnifyApps",
			"title":"Create list",
			"type":"ACTION"
		},
		{
			"additional":{
				"xsdSchemaConfig":{
					"root":{}
				},
				"visibleOptionalFields":[
					"root.parameters.dataSources.items.id",
					"root.parameters.dataSources.items.properties",
					"root.parameters.interfacePage.id",
					"root.parameters.interfacePage.version",
					"root.parameters.interfacePage.properties",
					"root.parameters.dataSources.items.version"
				]
			},
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":959,
				"resourceName":"code_by_unifyapps_javascript",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_uv02c-1",
			"id":"n_4gk0w",
			"index":3,
			"inputs":{
				"output":{
					"type":"object",
					"ua:__linkedVariable__":"n_4hXDW.outputs",
					"properties":{
						"items":{
							"type":"array",
							"title":"checks",
							"disableActions":true,
							"items":{
								"type":"object",
								"properties":{
									"check_id":{
										"type":"string",
										"title":"check_id"
									},
									"status":{
										"type":"string",
										"title":"status"
									},
									"targets":{
										"type":"array",
										"items":{
											"type":"object",
											"properties":{
												"targetId":{
													"type":"string",
													"title":"targetId"
												},
												"targetType":{
													"type":"string",
													"title":"targetType"
												}
											},
											"additionalProperties":false
										},
										"title":"targets"
									}
								}
							}
						}
					}
				},
				"input":{
					"additionalProperties":false,
					"type":"object",
					"ua:__linkedVariable__":"n_Ff9PD.outputs",
					"ua:modal":{
						"field_type":"STRING",
						"is_optional":true
					},
					"properties":{
						"check_id":{
							"type":"string",
							"title":"check_id"
						},
						"dataSources":{
							"type":"array",
							"items":{
								"type":"object",
								"properties":{
									"createdTime":{
										"type":"integer",
										"title":"createdTime"
									},
									"deleted":{
										"type":"boolean",
										"title":"deleted"
									},
									"deployedEntityVersion":{
										"type":"integer",
										"title":"deployedEntityVersion"
									},
									"entityType":{
										"type":"string",
										"title":"entityType"
									},
									"entityVersion":{
										"type":"integer",
										"title":"entityVersion"
									},
									"id":{
										"type":"string",
										"title":"id"
									},
									"lastModifiedBy":{
										"type":"integer",
										"title":"lastModifiedBy"
									},
									"modifiedTime":{
										"type":"integer",
										"title":"modifiedTime"
									},
									"ownerUserId":{
										"type":"integer",
										"title":"ownerUserId"
									},
									"properties":{
										"type":"object",
										"title":"properties"
									},
									"standard":{
										"type":"boolean",
										"title":"standard"
									},
									"version":{
										"type":"integer",
										"title":"version"
									}
								},
								"additionalProperties":false
							},
							"title":"dataSources"
						},
						"interfacePage":{
							"type":"object",
							"properties":{
								"createdTime":{
									"type":"integer",
									"title":"createdTime"
								},
								"deleted":{
									"type":"boolean",
									"title":"deleted"
								},
								"deploymentState":{
									"type":"object",
									"properties":{
										"deployedAt":{
											"type":"integer",
											"title":"deployedAt"
										},
										"deployedBy":{
											"type":"integer",
											"title":"deployedBy"
										},
										"entityVersion":{
											"type":"integer",
											"title":"entityVersion"
										},
										"version":{
											"type":"integer",
											"title":"version"
										}
									},
									"title":"deploymentState",
									"additionalProperties":false
								},
								"entityType":{
									"type":"string",
									"title":"entityType"
								},
								"id":{
									"type":"string",
									"title":"id"
								},
								"lastModifiedBy":{
									"type":"integer",
									"title":"lastModifiedBy"
								},
								"modifiedTime":{
									"type":"integer",
									"title":"modifiedTime"
								},
								"ownerUserId":{
									"type":"integer",
									"title":"ownerUserId"
								},
								"properties":{
									"type":"object",
									"title":"properties"
								},
								"standard":{
									"type":"boolean",
									"title":"standard"
								},
								"version":{
									"type":"integer",
									"title":"version"
								}
							},
							"title":"interfacePage",
							"additionalProperties":false
						}
					}
				},
				"code":"(() => {\n  // Both `interfacePage` and `dataSources` are assumed to be provided\n  var pageProps =\n    (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n  var page = pageProps || {};\n\n  function makeCheckResult(checkId, status, targets, debug) {\n    return {\n      check_id: checkId,\n      status: status,\n      targets: targets || [],\n      debug: debug || null,\n    };\n  }\n\n  function runCheckSafe(checkId, fn) {\n    try {\n      return fn(checkId);\n    } catch (e) {\n      return makeCheckResult(checkId, 'failed', [], {\n        error: String((e && e.message) || e),\n        stack: e && e.stack ? String(e.stack) : null,\n      });\n    }\n  }\n\n  // ============================================================\n  // Helper: detect if the current page is a global page\n  // ============================================================\n  function isGlobalPage() {\n    var pageId =\n      typeof interfacePage !== 'undefined' && interfacePage ? interfacePage.id : null;\n    return typeof pageId === 'string' && pageId.indexOf('e_global') === 0;\n  }\n\n\n  // ============================================================\n  // CHECKS — run for ALL pages (including global pages)\n  // ============================================================\n  var CHECKS = {\n\n    CHECK_103: function (args) {\n      var checkId = args.checkId;\n      var nameCounts = {};\n      var targets = [];\n      var seenEntities = {};\n\n      function trackName(name, id, type) {\n        if (!name || typeof name !== 'string') return;\n        var key = name.toLowerCase().trim();\n\n        // Prevent the same entity from being tracked twice\n        var entityKey = key + '|' + id + '|' + type;\n\n        if (seenEntities[entityKey]) {\n          return;\n        }\n\n        seenEntities[entityKey] = true;\n        if (!nameCounts[key]) {\n          nameCounts[key] = [];\n        }\n        nameCounts[key].push({ id: id, type: type, originalName: name });\n      }\n\n      function scanPageEntities(pageRecord, skipBlocks) {\n        if (!pageRecord || !pageRecord.properties) return;\n        var props = pageRecord.properties;\n\n        // 1. Blocks\n        if (!skipBlocks) {\n          var blocks = props.blocks || {};\n          for (var bId in blocks) {\n            trackName(blocks[bId].displayName, bId, 'block');\n          }\n        }\n\n        // 2. Variables\n        var vars = props.pageVariables || {};\n        for (var vId in vars) {\n          trackName(vars[vId].name, vId, 'pageVariable');\n        }\n\n        // 3. Functions\n        var fns = props.pageFunctions || {};\n        for (var fId in fns) {\n          trackName(fns[fId].name, fId, 'pageFunction');\n        }\n\n        // 4. Folders\n        var folders = props.folders || {};\n        for (var fldId in folders) {\n          trackName(folders[fldId].name, fldId, 'folder');\n        }\n\n        // 5. Walkthrough Steps\n        if (props.walkthrough && props.walkthrough.steps) {\n          var steps = props.walkthrough.steps;\n          for (var i = 0; i < steps.length; i++) {\n            trackName(steps[i].label, steps[i].id, 'walkthroughStep');\n          }\n        }\n      }\n\n      function scanDataSources(dsInput) {\n        if (!dsInput) return;\n        var list = (typeof dsInput.length === 'number') ? dsInput : Object.values(dsInput);\n        for (var i = 0; i < list.length; i++) {\n          var ds = list[i];\n          if (!ds) continue;\n          var dsName = (ds.properties && (ds.properties.name || (ds.properties.metadata && ds.properties.metadata.name)));\n          trackName(dsName, ds.id, 'dataSource');\n        }\n      }\n\n      // 1. Scan Local Page (active context)\n      if (typeof interfacePage !== 'undefined') scanPageEntities(interfacePage, false);\n\n      // 2. Scan Global Page (App context, skipping blocks)\n      if (typeof globalInterfacePage !== 'undefined') scanPageEntities(globalInterfacePage, true);\n\n      // 4. Scan Data Sources (both Page-level and Global-level)\n      if (typeof dataSources !== 'undefined') scanDataSources(dataSources);\n      if (typeof globalDataSources !== 'undefined') scanDataSources(globalDataSources);\n\n      // Evaluate collisions\n      for (var nameKey in nameCounts) {\n        var instances = nameCounts[nameKey];\n        if (instances.length > 1) {\n          for (var j = 0; j < instances.length; j++) {\n            targets.push({\n              targetId: instances[j].id,\n              targetType: instances[j].type\n            });\n          }\n        }\n      }\n\n      var status = targets.length ? 'failed' : 'passed';\n      return makeCheckResult(checkId, status, targets);\n    },\n\n    CHECK_101: (args) => {\n      var checkId = args.checkId;\n      var targets = [];\n     \n       \n      var OPEN = String.fromCharCode(123, 123);  \n      var CLOSE = String.fromCharCode(125, 125);\n      function stringHasNestedWhiskers(str) {\n        if (typeof str !== 'string') return false;\n      \n        var depth = 0;\n      \n        for (var i = 0; i < str.length - 1; i++) {\n          var pair = str.substring(i, i + 2);\n      \n          // Opening whisker\n          if (pair === OPEN) {\n            if (depth > 0) {\n              // Found a whisker while already inside one\n              return true;\n            }\n      \n            depth++;\n            i++; // skip second \n            continue;\n          }\n      \n          // Closing whisker\n          if (pair === CLOSE) {\n            if (depth > 0) {\n              depth--;\n            }\n      \n            i++; // skip second \n          }\n        }\n      \n        return false;\n      }\n\n      \n      function hasNestedWhiskers(value) {\n        if (value == null) return false;\n      \n        var t = typeof value;\n      \n        // String\n        if (t === 'string') {\n          return stringHasNestedWhiskers(value);\n        }\n      \n        // Array\n        if (Array.isArray(value)) {\n          for (var i = 0; i < value.length; i++) {\n            if (hasNestedWhiskers(value[i])) {\n              return true;\n            }\n          }\n      \n          return false;\n        }\n      \n        // Object\n        if (t === 'object') {\n          for (var k in value) {\n            try {\n              if (hasNestedWhiskers(value[k])) {\n                return true;\n              }\n            } catch (e) {\n              // ignore inaccessible fields\n            }\n          }\n        }\n      \n        return false;\n      }\n\n      var pageProps =\n        (typeof interfacePage !== 'undefined' &&\n          interfacePage &&\n          interfacePage.properties) || {};\n\n      // Blocks\n      var blocks = pageProps.blocks || {};\n      for (var bId in blocks) {\n        if (hasNestedWhiskers(blocks[bId])) {\n          targets.push({ targetId: bId, targetType: 'block' });\n        }\n      }\n\n      // Page Variables\n      var pVars = pageProps.pageVariables || {};\n      for (var vId in pVars) {\n        if (hasNestedWhiskers(pVars[vId])) {\n          targets.push({ targetId: vId, targetType: 'pageVariable' });\n        }\n      }\n\n      // Page Functions\n      var pFns = pageProps.pageFunctions || {};\n      for (var fId in pFns) {\n        if (hasNestedWhiskers(pFns[fId])) {\n          targets.push({ targetId: fId, targetType: 'pageFunction' });\n        }\n      }\n\n      // Data Sources\n      var dsRaw = (typeof dataSources !== 'undefined' && dataSources) || [];\n      var dsList = Array.isArray(dsRaw) ? dsRaw : Object.values(dsRaw);\n\n      for (var d = 0; d < dsList.length; d++) {\n        var ds = dsList[d];\n\n        if (ds && hasNestedWhiskers(ds)) {\n          targets.push({\n            targetId: ds.id,\n            targetType: 'dataSource',\n          });\n        }\n      }\n\n      return makeCheckResult(\n        checkId,\n        targets.length ? 'failed' : 'passed',\n        targets\n      );\n    },\n\n    CHECK_93: (args) => {\n      var checkId = args.checkId;\n\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n\n      // Data sources from global or from page props\n      var hasDataSrc = typeof dataSources !== 'undefined' && dataSources;\n      var dataSourceList;\n\n      if (hasDataSrc && typeof dataSources.length === 'number') {\n        dataSourceList = dataSources;\n      } else if (pageProps && pageProps.dataSources) {\n        var tmpList = [];\n        for (var key in pageProps.dataSources) {\n          tmpList.push(pageProps.dataSources[key]);\n        }\n        dataSourceList = tmpList;\n      } else {\n        dataSourceList = [];\n      }\n\n      var totalDataSourceCount = dataSourceList.length;\n\n      var status = totalDataSourceCount > 50 ? 'failed' : 'passed';\n      var pageId = typeof interfacePage !== 'undefined' && interfacePage ? interfacePage.id : null;\n      var targets = [{ targetId: pageId, targetType: 'page' }];\n      return makeCheckResult(checkId, status, targets);\n    },\n\n    CHECK_72: (args) => {\n      var checkId = args.checkId;\n\n      // ---------------- Base context ----------------\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n\n      var blocks = pageProps && pageProps.blocks ? pageProps.blocks : {};\n      var pageVariables = pageProps && pageProps.pageVariables ? pageProps.pageVariables : {};\n      var pageFunctions = pageProps && pageProps.pageFunctions ? pageProps.pageFunctions : {};\n\n      // Data sources from global or from page props\n      var hasDataSrc = typeof dataSources !== 'undefined' && dataSources;\n      var dataSourceList;\n\n      if (hasDataSrc && typeof dataSources.length === 'number') {\n        dataSourceList = dataSources;\n      } else if (pageProps && pageProps.dataSources) {\n        var tmpList = [];\n        for (var key in pageProps.dataSources) {\n          tmpList.push(pageProps.dataSources[key]);\n        }\n        dataSourceList = tmpList;\n      } else {\n        dataSourceList = [];\n      }\n\n      var targets = [];\n      var needle = \"localStorage['values']\";\n\n      // ---------------- Utility: deep string search ----------------\n      function containsNeedle(value, n) {\n        if (value == null) return false;\n\n        var t = typeof value;\n\n        // Direct string\n        if (t === 'string') {\n          return value.indexOf(n) !== -1;\n        }\n\n        // Array / array-like\n        if (t === 'object' && typeof value.length === 'number') {\n          for (var i = 0; i < value.length; i++) {\n            if (containsNeedle(value[i], n)) return true;\n          }\n          return false;\n        }\n\n        // Plain object / Java map / JSONObject\n        if (t === 'object') {\n          for (var k in value) {\n            try {\n              if (containsNeedle(value[k], n)) return true;\n            } catch (e) {\n              // ignore and continue\n            }\n          }\n        }\n\n        return false;\n      }\n\n      // ---------------- 1. Scan blocks ----------------\n      for (var blockId in blocks) {\n        var block = blocks[blockId];\n        if (!block || !block.component) continue;\n\n        if (containsNeedle(block, needle)) {\n          targets.push({ targetId: blockId, targetType: 'block' });\n        }\n      }\n\n      // ---------------- 2. Scan data sources ----------------\n      for (var i = 0; i < dataSourceList.length; i++) {\n        var ds = dataSourceList[i];\n        if (!ds) continue;\n\n        if (containsNeedle(ds, needle)) {\n          targets.push({\n            targetId: ds.id,\n            targetType: 'dataSource',\n          });\n        }\n      }\n\n      // ---------------- 3. Scan page variables ----------------\n      for (var varId in pageVariables) {\n        var variable = pageVariables[varId];\n        if (containsNeedle(variable, needle)) {\n          targets.push({\n            targetId: varId,\n            targetType: 'pageVariable',\n          });\n        }\n      }\n\n      // ---------------- 4. Scan page functions ----------------\n      for (var funcId in pageFunctions) {\n        var func = pageFunctions[funcId];\n        if (containsNeedle(func, needle)) {\n          targets.push({\n            targetId: funcId,\n            targetType: 'pageFunction',\n          });\n        }\n      }\n\n      // ---------------- 5. Result ----------------\n      var status = targets.length ? 'failed' : 'passed';\n      return makeCheckResult(checkId, status, targets);\n    },\n\n    CHECK_67: (args) => {\n      var checkId = args.checkId;\n\n      // ---------------- Base context ----------------\n      var pageProps =\n        (typeof interfacePage !== 'undefined' &&\n          interfacePage &&\n          interfacePage.properties) ||\n        null;\n\n      var pageVariables =\n        pageProps && pageProps.pageVariables ? pageProps.pageVariables : {};\n\n      var targets = [];\n\n      // Detect datasource-based initialization like:\n      function isDataSourceMapped(value) {\n        if (typeof value !== 'string') return false;\n\n        var v = value.trim();\n\n        // must be a moustache expression\n        if (v.indexOf('}}') === -1 || v.indexOf('{{') === -1) return false;\n\n        // must reference a datasource (e_xxx)\n        return /e_[a-zA-Z0-9]+\\s*\\[/.test(v);\n      }\n\n      // ---------------- Scan page variables ----------------\n      for (var varId in pageVariables) {\n        var variable = pageVariables[varId];\n        if (!variable) continue;\n\n        if (isDataSourceMapped(variable.initialValue)) {\n          targets.push({\n            targetId: varId,\n            targetType: 'pageVariable',\n          });\n        }\n      }\n\n      var status = targets.length ? 'failed' : 'passed';\n\n      return makeCheckResult(checkId, status, targets);\n    },\n\n    CHECK_50: (args) => {\n      var checkId = args.checkId;\n\n      // -------------------------------------------------------------------\n      // Helpers\n      // -------------------------------------------------------------------\n      function asArray(v) {\n        return v && typeof v.length === 'number' ? v : [];\n      }\n\n      // Recursively checks delayDuration > 200 across:\n      // - action.delayDuration\n      // - nested action.onSuccessActions[]\n      // - nested action.onErrorActions[]\n      function hasDelay(action) {\n        if (!action) return false;\n\n        // Own delay\n        var delay = action.delayDuration;\n        if (typeof delay === 'number' && delay > 200) return true;\n\n        // Nested: onSuccessActions\n        var succ = asArray(action.onSuccessActions);\n        for (var i = 0; i < succ.length; i++) {\n          if (hasDelay(succ[i])) return true;\n        }\n\n        // Nested: onErrorActions\n        var err = asArray(action.onErrorActions);\n        for (var j = 0; j < err.length; j++) {\n          if (hasDelay(err[j])) return true;\n        }\n\n        return false;\n      }\n\n      function safePush(targets, id, type) {\n        for (var i = 0; i < targets.length; i++) {\n          if (targets[i].targetId === id && targets[i].targetType === type) {\n            return;\n          }\n        }\n        targets.push({ targetId: id, targetType: type });\n      }\n\n      var targets = [];\n\n      // -------------------------------------------------------------------\n      // 1) Scan BLOCKS (block.events)\n      // -------------------------------------------------------------------\n      var blocks =\n        (typeof interfacePage !== 'undefined' &&\n          interfacePage &&\n          interfacePage.properties &&\n          interfacePage.properties.blocks) ||\n        {};\n\n      for (var blockId in blocks) {\n        var block = blocks[blockId];\n        if (!block) continue;\n\n        var events = asArray(block.events);\n        for (var i = 0; i < events.length; i++) {\n          var ev = events[i];\n          var action = ev && ev.action;\n          if (hasDelay(action)) {\n            safePush(targets, blockId, 'block');\n            break;\n          }\n        }\n      }\n\n      // -------------------------------------------------------------------\n      // 2) Scan PAGE-LEVEL INTERACTIONS\n      // -------------------------------------------------------------------\n      var interactions =\n        (typeof interfacePage !== 'undefined' &&\n          interfacePage &&\n          interfacePage.properties &&\n          interfacePage.properties.interactions) ||\n        {};\n\n      var pageEvents = asArray(interactions.events);\n      for (var p = 0; p < pageEvents.length; p++) {\n        var pe = pageEvents[p];\n        if (pe && hasDelay(pe.action)) {\n          var pageId =\n            typeof interfacePage !== 'undefined' && interfacePage ? interfacePage.id : null;\n          safePush(targets, pageId, 'page');\n          break;\n        }\n      }\n\n      var kbShortcuts = asArray(interactions.keyboardShortcuts);\n      for (var k = 0; k < kbShortcuts.length; k++) {\n        var ke = kbShortcuts[k];\n        if (ke && hasDelay(ke.action)) {\n          var pageId =\n            typeof interfacePage !== 'undefined' && interfacePage ? interfacePage.id : null;\n          safePush(targets, pageId, 'page');\n          break;\n        }\n      }\n\n      // -------------------------------------------------------------------\n      // 3) Scan DATA SOURCES (successEvents + failureEvents)\n      // -------------------------------------------------------------------\n      var hasDataSrc = typeof dataSources !== 'undefined' && dataSources;\n      var dsList;\n\n      if (hasDataSrc && typeof dataSources.length === 'number') {\n        dsList = dataSources;\n      } else if (pageProps && pageProps.dataSources) {\n        var tmpList = [];\n        for (var key in pageProps.dataSources) {\n          tmpList.push(pageProps.dataSources[key]);\n        }\n        dsList = tmpList;\n      } else {\n        dsList = [];\n      }\n\n      for (var d = 0; d < dsList.length; d++) {\n        var ds = dsList[d];\n        if (!ds || !ds.properties) continue;\n\n        var cb = ds.properties.callbacks || {};\n        var successEv = asArray(cb.successEvents);\n        var failureEv = asArray(cb.failureEvents);\n        var all = successEv.concat(failureEv);\n\n        for (var s = 0; s < all.length; s++) {\n          var evt = all[s];\n          var act = evt && evt.action;\n          if (hasDelay(act)) {\n            safePush(targets, ds.id, 'dataSource');\n            break;\n          }\n        }\n      }\n\n      // -------------------------------------------------------------------\n      // Final Result\n      // -------------------------------------------------------------------\n      var status = targets.length ? 'failed' : 'passed';\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_42: (args) => {\n      var checkId = args.checkId;\n\n      // ---------------- Base context ----------------\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n\n      var blocks = pageProps && pageProps.blocks ? pageProps.blocks : {};\n      var pageVariables = pageProps && pageProps.pageVariables ? pageProps.pageVariables : {};\n      var pageFunctions = pageProps && pageProps.pageFunctions ? pageProps.pageFunctions : {};\n\n      // Data sources from global or from page props\n      var hasDataSrc = typeof dataSources !== 'undefined' && dataSources;\n      var dataSourceList;\n\n      if (hasDataSrc && typeof dataSources.length === 'number') {\n        dataSourceList = dataSources;\n      } else if (pageProps && pageProps.dataSources) {\n        var tmpList = [];\n        for (var key in pageProps.dataSources) {\n          tmpList.push(pageProps.dataSources[key]);\n        }\n        dataSourceList = tmpList;\n      } else {\n        dataSourceList = [];\n      }\n\n      var targets = [];\n      var needle = '__PAGE_VARS__';\n\n      // ---------------- Utility: deep string search ----------------\n      function containsNeedle(value, n) {\n        if (value == null) return false;\n\n        var t = typeof value;\n\n        // Direct string\n        if (t === 'string') {\n          return value.indexOf(n) !== -1;\n        }\n\n        // Array / array-like\n        if (t === 'object' && typeof value.length === 'number') {\n          for (var i = 0; i < value.length; i++) {\n            if (containsNeedle(value[i], n)) return true;\n          }\n          return false;\n        }\n\n        // Plain object / Java map / JSONObject\n        if (t === 'object') {\n          for (var k in value) {\n            try {\n              if (containsNeedle(value[k], n)) return true;\n            } catch (e) {\n              // ignore and continue\n            }\n          }\n        }\n\n        return false;\n      }\n\n      // ---------------- 1. Scan blocks ----------------\n      for (var blockId in blocks) {\n        var block = blocks[blockId];\n        if (!block || !block.component) continue;\n\n        if (containsNeedle(block, needle)) {\n          targets.push({ targetId: blockId, targetType: 'block' });\n        }\n      }\n\n      // ---------------- 2. Scan data sources ----------------\n      for (var i = 0; i < dataSourceList.length; i++) {\n        var ds = dataSourceList[i];\n        if (!ds) continue;\n\n        if (containsNeedle(ds, needle)) {\n          targets.push({\n            targetId: ds.id,\n            targetType: 'dataSource',\n          });\n        }\n      }\n\n      // ---------------- 3. Scan page variables ----------------\n      for (var varId in pageVariables) {\n        var variable = pageVariables[varId];\n        if (containsNeedle(variable, needle)) {\n          targets.push({\n            targetId: varId,\n            targetType: 'pageVariable',\n          });\n        }\n      }\n\n      // ---------------- 4. Scan page functions ----------------\n      for (var funcId in pageFunctions) {\n        var func = pageFunctions[funcId];\n        if (containsNeedle(func, needle)) {\n          targets.push({\n            targetId: funcId,\n            targetType: 'pageFunction',\n          });\n        }\n      }\n\n      // ---------------- 5. Result ----------------\n      var status = targets.length ? 'failed' : 'passed';\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_43: (args) => {\n      var checkId = args.checkId;\n\n      // ---------------- Base context ----------------\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n\n      var blocks = pageProps && pageProps.blocks ? pageProps.blocks : {};\n\n      // Data sources from global or from page props\n      var hasDataSrc = typeof dataSources !== 'undefined' && dataSources;\n      var dataSourceList;\n\n      if (hasDataSrc && typeof dataSources.length === 'number') {\n        dataSourceList = dataSources;\n      } else if (pageProps && pageProps.dataSources) {\n        var tmpList = [];\n        for (var key in pageProps.dataSources) {\n          tmpList.push(pageProps.dataSources[key]);\n        }\n        dataSourceList = tmpList;\n      } else {\n        dataSourceList = [];\n      }\n\n      var pageVariables = pageProps && pageProps.pageVariables ? pageProps.pageVariables : {};\n      var pageFunctions = pageProps && pageProps.pageFunctions ? pageProps.pageFunctions : {};\n\n      var targets = [];\n      var needle = '__PAGE_FUNCTIONS__';\n\n      // ---------------- Utility: deep string search ----------------\n      function containsNeedle(value, n) {\n        if (value == null) return false;\n\n        var t = typeof value;\n\n        // Direct string\n        if (t === 'string') {\n          return value.indexOf(n) !== -1;\n        }\n\n        // Array / array-like\n        if (t === 'object' && typeof value.length === 'number') {\n          for (var i = 0; i < value.length; i++) {\n            if (containsNeedle(value[i], n)) return true;\n          }\n          return false;\n        }\n\n        // Plain object / Java map / JSONObject\n        if (t === 'object') {\n          for (var k in value) {\n            try {\n              if (containsNeedle(value[k], n)) return true;\n            } catch (e) {\n              // ignore and continue\n            }\n          }\n        }\n\n        return false;\n      }\n\n      // ---------------- 1. Scan blocks ----------------\n      for (var blockId in blocks) {\n        var block = blocks[blockId];\n        if (!block || !block.component) continue;\n\n        if (containsNeedle(block, needle)) {\n          targets.push({\n            targetId: blockId,\n            targetType: 'block',\n          });\n        }\n      }\n\n      // ---------------- 2. Scan data sources ----------------\n      for (var i = 0; i < dataSourceList.length; i++) {\n        var ds = dataSourceList[i];\n        if (!ds) continue;\n\n        if (containsNeedle(ds, needle)) {\n          targets.push({\n            targetId: ds.id,\n            targetType: 'dataSource',\n          });\n        }\n      }\n\n      // ---------------- 3. Scan page variables ----------------\n      for (var varId in pageVariables) {\n        var variable = pageVariables[varId];\n        if (containsNeedle(variable, needle)) {\n          targets.push({\n            targetId: varId,\n            targetType: 'pageVariable',\n          });\n        }\n      }\n\n      // ---------------- 4. Scan page functions ----------------\n      for (var funcId in pageFunctions) {\n        var func = pageFunctions[funcId];\n        if (containsNeedle(func, needle)) {\n          targets.push({\n            targetId: funcId,\n            targetType: 'pageFunction',\n          });\n        }\n      }\n\n      // ---------------- 5. Result ----------------\n      var status = targets.length ? 'failed' : 'passed';\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_37: (args) => {\n      var checkId = args.checkId;\n\n      // ---------------- Base context ----------------\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n\n      var blocks = pageProps && pageProps.blocks ? pageProps.blocks : {};\n\n      // Data sources from global or from page props\n      var hasDataSrc = typeof dataSources !== 'undefined' && dataSources;\n      var dataSourceList;\n\n      if (hasDataSrc && typeof dataSources.length === 'number') {\n        dataSourceList = dataSources;\n      } else if (pageProps && pageProps.dataSources) {\n        var tmpList = [];\n        for (var key in pageProps.dataSources) {\n          tmpList.push(pageProps.dataSources[key]);\n        }\n        dataSourceList = tmpList;\n      } else {\n        dataSourceList = [];\n      }\n\n      var targets = [];\n\n      // Check blocks for setFieldValue method in events\n      for (var blockId in blocks) {\n        var block = blocks[blockId];\n        if (!block || !block.events) continue;\n\n        var events = block.events || [];\n\n        for (var i = 0; i < events.length; i++) {\n          var event = events[i];\n          if (\n            event &&\n            event.action &&\n            event.action.payload &&\n            event.action.payload.methodName === 'setFieldValue'\n          ) {\n            // Flag the block using setFieldValue and suggest using setFieldValues\n            targets.push({ targetId: blockId, targetType: 'block' });\n            break;\n          }\n        }\n      }\n\n      // Check data sources for setFieldValue method in success/failure events\n      for (var i = 0; i < dataSourceList.length; i++) {\n        var dataSource = dataSourceList[i];\n        if (!dataSource || !dataSource.properties) continue;\n\n        var cb = dataSource.properties.callbacks || {};\n        var successEvents = cb.successEvents || [];\n        var failureEvents = cb.failureEvents || [];\n        var allEvents = successEvents.concat(failureEvents);\n\n        for (var j = 0; j < allEvents.length; j++) {\n          var event = allEvents[j];\n          if (\n            event &&\n            event.action &&\n            event.action.actionType === 'controlBlockMethod' &&\n            event.action.payload &&\n            event.action.payload.methodName === 'setFieldValue'\n          ) {\n            targets.push({ targetId: dataSource.id, targetType: 'dataSource' });\n            break;\n          }\n        }\n      }\n\n      var status = targets.length ? 'failed' : 'passed';\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_36: (args) => {\n      var checkId = args.checkId;\n\n      // ---------------- Base context ----------------\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n\n      var blocks = pageProps && pageProps.blocks ? pageProps.blocks : {};\n\n      // Data sources from global or from page props\n      var hasDataSrc = typeof dataSources !== 'undefined' && dataSources;\n      var dataSourceList;\n\n      if (hasDataSrc && typeof dataSources.length === 'number') {\n        dataSourceList = dataSources;\n      } else if (pageProps && pageProps.dataSources) {\n        var tmpList = [];\n        for (var key in pageProps.dataSources) {\n          tmpList.push(pageProps.dataSources[key]);\n        }\n        dataSourceList = tmpList;\n      } else {\n        dataSourceList = [];\n      }\n\n      var targets = [];\n\n      // Check blocks for resetField method in events\n      for (var blockId in blocks) {\n        var block = blocks[blockId];\n        if (!block || !block.events) continue;\n\n        var events = block.events || [];\n\n        for (var i = 0; i < events.length; i++) {\n          var event = events[i];\n          if (\n            event &&\n            event.action &&\n            event.action.actionType === 'controlBlockMethod' &&\n            event.action.payload &&\n            event.action.payload.methodName === 'resetField'\n          ) {\n            targets.push({ targetId: blockId, targetType: 'block' });\n            break;\n          }\n        }\n      }\n\n      // Check data sources for resetField method in success/failure events\n      for (var i = 0; i < dataSourceList.length; i++) {\n        var dataSource = dataSourceList[i];\n        if (!dataSource || !dataSource.properties) continue;\n\n        var cb = dataSource.properties.callbacks || {};\n        var successEvents = cb.successEvents || [];\n        var failureEvents = cb.failureEvents || [];\n        var allEvents = successEvents.concat(failureEvents);\n\n        for (var j = 0; j < allEvents.length; j++) {\n          var event = allEvents[j];\n          if (\n            event &&\n            event.action &&\n            event.action.actionType === 'controlBlockMethod' &&\n            event.action.payload &&\n            event.action.payload.methodName === 'resetField'\n          ) {\n            targets.push({ targetId: dataSource.id, targetType: 'dataSource' });\n            break;\n          }\n        }\n      }\n\n      var status = targets.length ? 'failed' : 'passed';\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_33: (args) => {\n      var checkId = args.checkId;\n\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n      var pageVariables = pageProps && pageProps.pageVariables ? pageProps.pageVariables : {};\n      var targets = [];\n\n      for (var varId in pageVariables) {\n        var variable = pageVariables[varId];\n        // Check for generic variable names like 'variable1', 'variable2', etc.\n        if (variable && variable.name && /variable\\d+/.test(variable.name)) {\n          targets.push({ targetId: varId, targetType: 'pageVariable' });\n        }\n      }\n\n      var status = targets.length ? 'failed' : 'passed';\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_34: (args) => {\n      var checkId = args.checkId;\n\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n      var pageFunctions = pageProps && pageProps.pageFunctions ? pageProps.pageFunctions : {};\n      var targets = [];\n\n      for (var funcId in pageFunctions) {\n        var func = pageFunctions[funcId];\n        // Check for generic function names like 'function1', 'function2', etc.\n        if (func && func.name && /function\\d+/.test(func.name)) {\n          targets.push({ targetId: funcId, targetType: 'pageFunction' });\n        }\n      }\n\n      var status = targets.length ? 'failed' : 'passed';\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_35: (args) => {\n      var checkId = args.checkId;\n\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n\n      // Data sources from global or from page props\n      var hasDataSrc = typeof dataSources !== 'undefined' && dataSources;\n      var dataSourceList;\n\n      if (hasDataSrc && typeof dataSources.length === 'number') {\n        dataSourceList = dataSources;\n      } else if (pageProps && pageProps.dataSources) {\n        var tmpList = [];\n        for (var key in pageProps.dataSources) {\n          tmpList.push(pageProps.dataSources[key]);\n        }\n        dataSourceList = tmpList;\n      } else {\n        dataSourceList = [];\n      }\n\n      var targets = [];\n\n      for (var i = 0; i < dataSourceList.length; i++) {\n        var dataSource = dataSourceList[i];\n        // Check for generic data source names like 'dataSource1', 'dataSource2', etc.\n        if (\n          dataSource &&\n          dataSource.properties &&\n          dataSource.properties.name &&\n          /dataSource\\d+/.test(dataSource.properties.name)\n        ) {\n          targets.push({ targetId: dataSource.id, targetType: 'dataSource' });\n        }\n      }\n\n      var status = targets.length ? 'failed' : 'passed';\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_30: (args) => {\n      var checkId = args.checkId;\n\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n\n      // Data sources from global or from page props\n      var hasDataSrc = typeof dataSources !== 'undefined' && dataSources;\n      var dataSourceList;\n\n      if (hasDataSrc && typeof dataSources.length === 'number') {\n        dataSourceList = dataSources;\n      } else if (pageProps && pageProps.dataSources) {\n        var tmpList = [];\n        for (var key in pageProps.dataSources) {\n          tmpList.push(pageProps.dataSources[key]);\n        }\n        dataSourceList = tmpList;\n      } else {\n        dataSourceList = [];\n      }\n\n      var pageVariables = pageProps && pageProps.pageVariables ? pageProps.pageVariables : {};\n      var pageFunctions = pageProps && pageProps.pageFunctions ? pageProps.pageFunctions : {};\n\n      // Calculate the total number of resources (data sources, page variables, and page functions)\n      var totalResources = dataSourceList.length;\n\n      // Counting the number of page variables\n      for (var varId in pageVariables) {\n        if (pageVariables.hasOwnProperty(varId)) {\n          totalResources++;\n        }\n      }\n\n      // Counting the number of page functions\n      for (var funcId in pageFunctions) {\n        if (pageFunctions.hasOwnProperty(funcId)) {\n          totalResources++;\n        }\n      }\n\n      var targets = [];\n\n      // If there are more than 10 resources, check if folder organization is applied\n      if (totalResources > 10) {\n        var resourcesPerFolder = 5;\n        var folderCount = Math.ceil(totalResources / resourcesPerFolder);\n\n        // If the resources are not organized in folders, flag the issue\n        if (folderCount > 1) {\n          // Target the page by its ID\n          var pageId =\n            typeof interfacePage !== 'undefined' && interfacePage ? interfacePage.id : null;\n          targets.push({ targetId: pageId, targetType: 'page' });\n        }\n      }\n\n      var status = targets.length ? 'failed' : 'passed';\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_25: (args) => {\n      var checkId = args.checkId;\n      var failedIds = [];\n\n      // --------------------------------------------\n      // 1. Check BLOCK events\n      // --------------------------------------------\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n      var blocks = pageProps && pageProps.blocks ? pageProps.blocks : {};\n\n      for (var blockId in blocks) {\n        var block = blocks[blockId];\n        if (!block || !block.component) continue;\n\n        var comp = block.component;\n        var events = comp.events || [];\n\n        for (var i = 0; i < events.length; i++) {\n          var evt = events[i];\n          if (!evt || !evt.action) continue;\n\n          var action = evt.action;\n          var actionType = action.actionType;\n          var payload = action.payload;\n\n          // Empty controlDataSource action → FAIL if either dataSourceId or method is missing\n          if (\n            actionType === 'controlDataSource' &&\n            (!payload || !payload.dataSourceId || !payload.method || payload.method === '')\n          ) {\n            failedIds.push(blockId);\n            break;\n          }\n        }\n      }\n\n      // --------------------------------------------\n      // 2. Check DATA SOURCE callbacks (successEvents + failureEvents)\n      // --------------------------------------------\n      var hasDataSrc = typeof dataSources !== 'undefined' && dataSources;\n      var dsList;\n\n      if (hasDataSrc && typeof dataSources.length === 'number') {\n        dsList = dataSources;\n      } else if (pageProps && pageProps.dataSources) {\n        var tmpList = [];\n        for (var key in pageProps.dataSources) {\n          tmpList.push(pageProps.dataSources[key]);\n        }\n        dsList = tmpList;\n      } else {\n        dsList = [];\n      }\n\n      for (var d = 0; d < dsList.length; d++) {\n        var ds = dsList[d];\n        if (!ds || !ds.properties) continue;\n\n        var cb = ds.properties.callbacks || {};\n        var successEv = cb.successEvents || [];\n        var failureEv = cb.failureEvents || [];\n\n        // Combine both arrays\n        var allEv = successEv.concat(failureEv);\n\n        for (var k = 0; k < allEv.length; k++) {\n          var ev = allEv[k];\n          if (!ev || !ev.action) continue;\n\n          var a = ev.action;\n          var at = a.actionType;\n          var p = a.payload;\n\n          // Empty controlDataSource action → FAIL if either dataSourceId or method is missing\n          if (\n            at === 'controlDataSource' &&\n            (!p || !p.dataSourceId || !p.method || p.method === '')\n          ) {\n            // We mark by ds.id but DS are not blocks, so use targetType=dataSource\n            failedIds.push(ds.id);\n            break;\n          }\n        }\n      }\n\n      // --------------------------------------------\n      // Finalize\n      // --------------------------------------------\n      var status = failedIds.length ? 'failed' : 'passed';\n      var targets = [];\n\n      for (var x = 0; x < failedIds.length; x++) {\n        var id = failedIds[x];\n        var type = blocks[id] ? 'block' : 'dataSource';\n        targets.push({ targetId: id, targetType: type });\n      }\n\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_8: (args) => {\n      var checkId = args.checkId;\n\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n      var blocks = pageProps && pageProps.blocks ? pageProps.blocks : {};\n\n      var totalBlockCount = 0;\n      for (var _ in blocks) {\n        totalBlockCount++;\n      }\n\n      var status = totalBlockCount > 300 ? 'failed' : 'passed';\n      var pageId = typeof interfacePage !== 'undefined' && interfacePage ? interfacePage.id : null;\n      var targets = [{ targetId: pageId, targetType: 'page' }];\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_3: (args) => {\n      var checkId = args.checkId;\n      var hiddenBlockIds = [];\n\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n      var baseBlocks = pageProps && pageProps.blocks ? pageProps.blocks : {};\n      var devices = pageProps && pageProps.devices ? pageProps.devices : {};\n\n      for (var blockId in baseBlocks) {\n        if (blockId === 'header_id' || blockId === 'footer_id') continue;\n        var block = baseBlocks[blockId];\n        if (!block) continue;\n\n        var baseVisibility = block && block.visibility && block.visibility.value;\n        if (baseVisibility !== false) continue;\n\n        var visibleOrConditional = false;\n        for (var deviceName in devices) {\n          var device = devices[deviceName];\n          var deviceBlock =\n            device && device.blocks && device.blocks[blockId] ? device.blocks[blockId] : null;\n          if (!deviceBlock) continue;\n          var v = deviceBlock.visibility && deviceBlock.visibility.value;\n          if (v === true || v === 'conditions') {\n            visibleOrConditional = true;\n            break;\n          }\n        }\n\n        if (!visibleOrConditional) {\n          hiddenBlockIds.push(blockId);\n        }\n      }\n\n      var status = hiddenBlockIds.length ? 'failed' : 'passed';\n      var targets = [];\n\n      for (var i = 0; i < hiddenBlockIds.length; i++) {\n        targets.push({\n          targetId: hiddenBlockIds[i],\n          targetType: 'block',\n        });\n      }\n\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_15: (args) => {\n      var checkId = args.checkId;\n      var flaggedIds = [];\n\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n      var baseBlocks = pageProps && pageProps.blocks ? pageProps.blocks : {};\n      var devices = pageProps && pageProps.devices ? pageProps.devices : {};\n\n      // Check base blocks\n      for (var blockId in baseBlocks) {\n        var block = baseBlocks[blockId];\n        if (!block) continue;\n\n        var rotation =\n          block &&\n            block.component &&\n            block.component.appearance &&\n            block.component.appearance.styles &&\n            block.component.appearance.styles.rotation &&\n            block.component.appearance.styles.rotation.custom\n            ? block.component.appearance.styles.rotation.custom\n            : null;\n\n        if (rotation === '0deg' || rotation === '0°' || rotation === '0') {\n          flaggedIds.push(blockId);\n        }\n      }\n\n      // Check all device-level overrides\n      for (var deviceName in devices) {\n        var device = devices[deviceName];\n        var deviceBlocks = device && device.blocks ? device.blocks : {};\n\n        for (var blockId in deviceBlocks) {\n          var block = deviceBlocks[blockId];\n          if (!block) continue;\n\n          var rotation =\n            block &&\n              block.component &&\n              block.component.appearance &&\n              block.component.appearance.styles &&\n              block.component.appearance.styles.rotation &&\n              block.component.appearance.styles.rotation.custom\n              ? block.component.appearance.styles.rotation.custom\n              : null;\n\n          if (rotation === '0deg' || rotation === '0°' || rotation === '0') {\n            // avoid duplicates\n            var found = false;\n            for (var j = 0; j < flaggedIds.length; j++) {\n              if (flaggedIds[j] === blockId) {\n                found = true;\n                break;\n              }\n            }\n            if (!found) {\n              flaggedIds.push(blockId);\n            }\n          }\n        }\n      }\n\n      var status = flaggedIds.length > 0 ? 'failed' : 'passed';\n      var targets = [];\n\n      for (var i = 0; i < flaggedIds.length; i++) {\n        targets.push({\n          targetId: flaggedIds[i],\n          targetType: 'block',\n        });\n      }\n\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_16: (args) => {\n      var checkId = args.checkId;\n      var flagged = [];\n\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n      var blocks = pageProps && pageProps.blocks ? pageProps.blocks : {};\n\n      function hasForcedScroll(overflowObj) {\n        if (!overflowObj) return false;\n\n        // stringify only the overflow object (safe + correct)\n        var str = '';\n        try {\n          if (typeof overflowObj.toJSONString === 'function') {\n            str = overflowObj.toJSONString();\n          } else {\n            str = JSON.stringify(overflowObj);\n          }\n        } catch (e) {\n          str = String(overflowObj);\n        }\n\n        str = str.toLowerCase();\n\n        // Detect any forced scroll (horizontal, vertical, or universal)\n        // We intentionally do NOT check for \"auto\" – auto is acceptable\n        return (\n          str.indexOf('overflow-scroll') !== -1 ||\n          str.indexOf('overflow-x-scroll') !== -1 ||\n          str.indexOf('overflow-y-scroll') !== -1\n        );\n      }\n\n      for (var blockId in blocks) {\n        var block = blocks[blockId];\n        if (!block) continue;\n\n        var styles =\n          block &&\n            block.component &&\n            block.component.appearance &&\n            block.component.appearance.styles\n            ? block.component.appearance.styles\n            : null;\n        if (!styles) continue;\n\n        var overflow = styles.overflow;\n        if (hasForcedScroll(overflow)) {\n          flagged.push(blockId);\n        }\n      }\n\n      var status = flagged.length > 0 ? 'failed' : 'passed';\n      var targets = [];\n\n      for (var i = 0; i < flagged.length; i++) {\n        targets.push({\n          targetId: flagged[i],\n          targetType: 'block',\n        });\n      }\n\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_11: (args) => {\n      var checkId = args.checkId;\n\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n      var slug = (pageProps && pageProps.slug) || '';\n      var invalidSlugPattern = /^page[-_]\\w+$/i;\n      var badSlug = invalidSlugPattern.test(slug);\n      var status = badSlug ? 'failed' : 'passed';\n\n      var pageId = typeof interfacePage !== 'undefined' && interfacePage ? interfacePage.id : null;\n      var targets = [{ targetId: pageId, targetType: 'page' }];\n      return makeCheckResult(checkId, status, targets);\n    },\n    CHECK_10: (args) => {\n      var checkId = args.checkId;\n      var failedBlocks = [];\n      var failedFns = [];\n\n      function safeStringify(v) {\n        try {\n          if (v && typeof v.toJSONString === 'function') return v.toJSONString();\n          return JSON.stringify(v || '');\n        } catch (e) {\n          return '';\n        }\n      }\n\n      function arrayContains(arr, item) {\n        for (var i = 0; i < arr.length; i++) {\n          if (arr[i] === item) return true;\n        }\n        return false;\n      }\n\n      var pageProps =\n        (typeof interfacePage !== 'undefined' && interfacePage && interfacePage.properties) || null;\n      var blocks = pageProps && pageProps.blocks ? pageProps.blocks : {};\n\n      // Blocks\n      for (var blockId in blocks) {\n        var block = blocks[blockId];\n        if (!block) continue;\n\n        var events = block.events && typeof block.events.length === 'number' ? block.events : [];\n\n        for (var i = 0; i < events.length; i++) {\n          var evt = events[i];\n          if (!evt) continue;\n\n          var action = evt.action;\n          var code = safeStringify(action);\n          if (code.indexOf('console.') !== -1 || code.indexOf('debugger') !== -1) {\n            if (!arrayContains(failedBlocks, blockId)) {\n              failedBlocks.push(blockId);\n            }\n            break;\n          }\n        }\n      }\n\n      // Page functions\n      var funcs = pageProps && pageProps.pageFunctions ? pageProps.pageFunctions : {};\n      for (var funcId in funcs) {\n        var fn = funcs[funcId];\n        if (!fn) continue;\n\n        var body = '';\n        try {\n          var functionBody = fn.functionBody;\n          if (functionBody && typeof functionBody.toJSONString === 'function') {\n            body = functionBody.toJSONString();\n          } else {\n            body = String(functionBody || '');\n          }\n        } catch (e) {\n          body = '';\n        }\n\n        if (body.indexOf('console.') !== -1 || body.indexOf('debugger') !== -1) {\n          if (!arrayContains(failedFns, funcId)) {\n            failedFns.push(funcId);\n          }\n        }\n      }\n\n      var hasIssues = failedBlocks.length || failedFns.length;\n      var status = hasIssues ? 'failed' : 'passed';\n      var targets = [];\n\n      for (var i = 0; i < failedBlocks.length; i++) {\n        targets.push({\n          targetId: failedBlocks[i],\n          targetType: 'block',\n        });\n      }\n\n      for (var j = 0; j < failedFns.length; j++) {\n        targets.push({\n          targetId: failedFns[j],\n          targetType: 'function',\n        });\n      }\n\n      if (!hasIssues) {\n        var pageId =\n          typeof interfacePage !== 'undefined' && interfacePage ? interfacePage.id : null;\n        targets.push({ targetId: pageId, targetType: 'page' });\n      }\n\n      return makeCheckResult(checkId, status, targets);\n    },\n  };\n\n  // ============================================================\n  // NON_GLOBAL_CHECKS — skipped entirely for global pages (pages whose ID starts with \"e_global\")\n  // ============================================================\n  var NON_GLOBAL_CHECKS = {\n\n    CHECK_88: (args) => {\n      var checkId = args.checkId;\n\n      var pageProps =\n        (typeof interfacePage !== 'undefined' &&\n          interfacePage &&\n          interfacePage.properties) || null;\n\n      var documentTitle =\n        pageProps && typeof pageProps.documentTitle === 'string'\n          ? pageProps.documentTitle.trim()\n          : '';\n\n      var pageId =\n        typeof interfacePage !== 'undefined' && interfacePage\n          ? interfacePage.id\n          : null;\n\n      var targets = [];\n\n      // FAIL → if documentTitle is missing or empty\n      if (!documentTitle) {\n        targets.push({\n          targetId: pageId,\n          targetType: 'page',\n        });\n      }\n\n      var status = targets.length ? 'failed' : 'passed';\n\n      return makeCheckResult(checkId, status, targets);\n    },\n\n  }\n\n  // ============================================================\n  // Run all checks dynamically\n  // ============================================================\n  var checks = [];\n  for (var checkKey in CHECKS) {\n    if (CHECKS.hasOwnProperty(checkKey)) {\n      var checkFn = CHECKS[checkKey];\n      var result = runCheckSafe(checkKey, (checkId) => checkFn({ checkId: checkId }));\n      checks.push(result);\n    }\n  }\n\n  // Run NON_GLOBAL_CHECKS only for non-global pages\n  if (!isGlobalPage()) {\n    for (var nonGlobalKey in NON_GLOBAL_CHECKS) {\n      if (NON_GLOBAL_CHECKS.hasOwnProperty(nonGlobalKey)) {\n        var nonGlobalFn = NON_GLOBAL_CHECKS[nonGlobalKey];\n        var nonGlobalResult = runCheckSafe(nonGlobalKey, (checkId) => nonGlobalFn({ checkId: checkId }));\n        checks.push(nonGlobalResult);\n      }\n    }\n  }\n\n\n  return { items: checks };\n})();",
				"isAsync":false,
				"parameters":{
					"check_id":"{{ n_Ff9PD.outputs.check_id }}",
					"interfacePage":{
						"id":"{{ n_Ff9PD.outputs.interfacePage.id }}",
						"properties":"{{ n_Ff9PD.outputs.interfacePage.properties }}",
						"version":"{{ n_Ff9PD.outputs.interfacePage.version }}"
					},
					"dataSources":{
						"source":"{{ n_Ff9PD.outputs.dataSources }}",
						"ua:type":"mappedArray",
						"items":{
							"id":"{{ n_Ff9PD.outputs.dataSources[0].id }}",
							"properties":"{{ n_Ff9PD.outputs.dataSources[0].properties }}",
							"version":"{{ n_Ff9PD.outputs.dataSources[0].version }}"
						}
					}
				}
			},
			"skip":false,
			"subTitle":"Code by UnifyApps",
			"title":"Execute Javascript",
			"type":"ACTION"
		},
		{
			"additional":{
				"visibleOptionalFields":[
					"root.itemsToAdd.items.check_id",
					"root.itemsToAdd.items.status",
					"root.itemsToAdd.items.targets.items.targetId",
					"root.itemsToAdd.items.targets.items.targetType"
				]
			},
			"context":{
				"appName":"variable_by_unifyapps",
				"resourceVersion":990,
				"resourceName":"variable_by_unifyapps_add_items_to_list",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":true,
			"fallbackMode":"STOP",
			"groupId":"_uv02c-1",
			"id":"n_LUkEA",
			"index":4,
			"inputs":{
				"captureAddedItems":false,
				"insertLocation":"END",
				"itemsToAdd":{
					"ua:type":"mappedArray",
					"source":"{{ n_4gk0w.outputs.result.items }}",
					"items":{
						"check_id":"{{ n_4gk0w.outputs.result.items[0].check_id }}",
						"status":"{{ n_4gk0w.outputs.result.items[0].status }}",
						"targets":{
							"source":"{{ n_4gk0w.outputs.result.items[0].targets }}",
							"ua:type":"mappedArray",
							"items":{
								"targetId":"{{ n_4gk0w.outputs.result.items[0].targets[0].targetId }}",
								"targetType":"{{ n_4gk0w.outputs.result.items[0].targets[0].targetType }}"
							}
						}
					}
				},
				"listName":"{{ n_4hXDW.outputs.items[0] }}"
			},
			"skip":false,
			"subTitle":"Variable by UnifyApps",
			"title":"Add items to list",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"callables",
				"resourceVersion":1102,
				"resourceName":"callables_return_to_automation",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_uv02c-1",
			"id":"n_VdssN",
			"index":5,
			"inputs":{
				"result":{
					"checks":{
						"source":"{{ n_4hXDW.outputs.items }}",
						"ua:type":"mappedArray",
						"items":{
							"check_id":"{{ n_4hXDW.outputs.items[0].check_id }}",
							"status":"{{ n_4hXDW.outputs.items[0].status }}",
							"targets":{
								"source":"{{ n_4hXDW.outputs.items[0].targets }}",
								"ua:type":"mappedArray",
								"items":{
									"targetId":"{{ n_4hXDW.outputs.items[0].targets[0].targetId }}",
									"targetType":"{{ n_4hXDW.outputs.items[0].targets[0].targetType }}"
								}
							}
						}
					}
				}
			},
			"skip":false,
			"subTitle":"Callable",
			"title":"Respond to automation",
			"type":"STOP"
		}
	],
	"ownerUserId":27,
	"projectId":0,
	"schemaReferences":[
		
	],
	"settings":{
		"enableNodeLevelLogging":true,
		"enableRunLogging":true,
		"enableVariableLogging":true,
		"route":{
			"default":false,
			"tierName":"global"
		}
	},
	"standard":false,
	"tags":[
		"BPC"
	],
	"version":244
}